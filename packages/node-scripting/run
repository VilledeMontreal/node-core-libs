#!/usr/bin/env node
const caporal = require('@villedemontreal/caporal').program;

require(`${__dirname}/src/run`).run({
  caporal,
  projectRoot: __dirname,
  scriptsIndexModule: `./scripts/index`
});
